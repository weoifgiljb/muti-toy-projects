# GUI (Vue)

This Vue UI lives in `ui/` and is hosted by `gui.py` using `pywebview`.

## Build UI
```powershell
cd ui
npm install
npm run build
```

## Run GUI (desktop)
```powershell
python -m pip install pywebview
python gui.py
```

## Build GUI EXE
```powershell
.\build-gui.ps1
```
